# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build home-automation-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t home-automation-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system home-automation-api && \
          adduser --system -G home-automation-api home-automation-api

COPY dist/apps/home-automation-api home-automation-api/
RUN chown -R home-automation-api:home-automation-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix home-automation-api --omit=dev -f install

CMD [ "node", "home-automation-api" ]
